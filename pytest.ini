[pytest]
testpaths = code/tests
# Import mode 'importlib' avoids the project's top-level "code" package name
# colliding with Python's stdlib "code" module during test/conftest import.
addopts = --import-mode=importlib
